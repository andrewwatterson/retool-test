{
  "name": "tests",
  "private": true,
  "type": "module",
  "scripts": {
    "auth": "playwright test --project=auth --headed",
    "test": "playwright test --project=products",
    "test:headed": "playwright test --project=products --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
